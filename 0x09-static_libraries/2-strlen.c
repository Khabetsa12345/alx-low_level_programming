#include "main.h"

/**
* _strlen - Returns the length of a string
* @s: character type pointer
*
* Return: The length of @s
*/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		;
	}
	return (i);
}
